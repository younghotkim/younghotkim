function runMore(distance: number): number;
// function (parameter,arguments...): return type
function runMore(distance: number): number {
  return distance + 10;
}
console.log(runMore(20));

//function runMore < 함수명 (distance: number) <parameter : number < 결과값(return) {
