let pPhysicalDOM = document.createElement("p");
pPhysicalDOM.innerText = "Hello PhysicalDOM world!";

document.body.appendChild(pPhysicalDOM);

let spanPhysicalDOM = document.createElement("span");
spanPhysicalDOM.innerText = "Hello span world!";

document.body.appendChild(spanPhysicalDOM);
